name: Check Missing Attendance
on:
  workflow_dispatch:  # Solo esecuzione manuale

jobs:
  check-attendance:
    # Nessuna guardia necessaria: avvio solo manuale
    runs-on: ubuntu-latest
    steps:
      - name: Call attendance check function
        run: |
          curl -X POST \
            -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json" \
            "https://nohufgceuqhkycsdffqj.supabase.co/functions/v1/check-missing-attendance"
